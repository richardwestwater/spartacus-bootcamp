import { ShortProductPageMetaResolver } from './short-product-page-meta-resolver';

describe('ShortProductPageMetaResolver', () => {
  it('should create an instance', () => {
    expect(new ShortProductPageMetaResolver()).toBeTruthy();
  });
});
